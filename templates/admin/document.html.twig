{% extends 'base.html.twig' %}

{% block title %}Admin index{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 1100px; width: 200%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
        thead {color:indianred;}
        tbody {color:black;}
        tfoot {color:red;}
    </style>

    <div class="example-wrapper">

        <div class="dashboard-headline">
            <div class="table-responsive ">
                <table class="table table-shopping">

                    <thead>
                    <tr>
                        <th class="form-check">Document</th>
                        <th class="form-check">DateDepos</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for admin in admins %}
                        <tr>
                            <td class="form-check">{{ admin.document }}</td>
                            <td class="form-check">{{ admin.dateDepos ? admin.dateDepos|date('Y-m-d H:i:s') : '' }}</td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="4">Aucun document trouvé</td>
                        </tr>
                    {% endfor %}
                    </tbody>

                </table>
            </div>
        </div>
    </div>



{% endblock %}
