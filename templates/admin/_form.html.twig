{#{{ form_start(form) }}#}
{#    {{ form_widget(form) }}#}
{#    <button class="btn">{{ button_label|default('Save') }}</button>#}
{#{{ form_end(form) }}#}

{% block body %}

    <style>
        .example-wrapper { margin: auto; max-width: 1200px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #d2959b; padding: 2px 6px; }
    </style>

    {{ form_start(form) }}
    <div class="my-custom-class-for-errors">
        {{ form_errors(form) }}
    </div>

    <div class="form">
        <div class="form-group"> {{ form_widget(form) }}</div>
        <div class="col" id="some-custom-id">
            <button class="btn btn-default " style="background: #d2959b">{{ button_label|default('Ajouter dossier') }}</button>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}