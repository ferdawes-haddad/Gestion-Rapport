{% extends 'base.html.twig' %}

{% block title %}Edit Admin{% endblock %}

{% block body %}
    <h1>Modifier ce dossier</h1>

    {{ include('admin/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('admin_pdf') }}"class="btn btn-default " style="background: #d2959b">return à la liste</a>

    {{ include('admin/_delete_form.html.twig') }}
{% endblock %}
