{% extends 'base.html.twig' %}

{% block title %}Rapport{% endblock %}

{% block body %}

    <style>
        .example-wrapper { margin: auto; max-width: 500px; width: 10%; font: 20px/1.5 sans-serif; }
        .example-wrapper code { background: #d2959b; padding: 2px 6px; }
    </style>
    <h1>Rapport</h1>

<div class="form wrapper">
    <table class="table">
        <tbody>

            <tr>
                <div class="form-group"> <th>Titre</th></div>
                <div class="form-group"><td>{{ rapport.titre }}</td></div>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ rapport.description }}</td>
            </tr>

        </tbody>
    </table>
</div>


    <a href="{{ path('rapport_index') }}" class="btn btn-default glyphicon glyphicon-list" style="background: #5eb5e0"></a>
    <a href="{{ path('rapport_edit', {'id': rapport.id}) }}"  class="btn btn-default glyphicon glyphicon-edit" style="background: #5eb5e0"></a>
    <a>{{ include('rapport/_delete_form.html.twig') }}</a>
{% endblock %}
