{% extends 'base.html.twig' %}

{% block title %}Rapport{% endblock %}

{% block body %}
    <h1>Rapport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rapport.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ rapport.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ rapport.description }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ rapport.note }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rapport_index') }}">back to list</a>

    <a href="{{ path('rapport_edit', {'id': rapport.id}) }}">edit</a>

    {{ include('rapport/_delete_form.html.twig') }}
{% endblock %}
